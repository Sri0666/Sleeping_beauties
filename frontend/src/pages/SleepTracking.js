import React from 'react';
import { Typography, Box } from '@mui/material';

const SleepTracking = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Sleep Tracking
      </Typography>
      <Typography variant="body1">
        Detailed sleep tracking interface coming soon...
      </Typography>
    </Box>
  );
};

export default SleepTracking;